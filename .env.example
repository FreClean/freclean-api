# Copy to .env and fill in real values. Never commit the real .env file.

NODE_ENV=development
PORT=4000

# Generate a strong random value for production — do not reuse this example.
JWT_SECRET=CHANGE_ME_IN_PRODUCTION_ONLY_DEMO_SECRET
JWT_EXPIRES_IN=8h

CORS_ORIGIN=*

RATE_LIMIT_WINDOW_MS=900000
RATE_LIMIT_MAX=300
